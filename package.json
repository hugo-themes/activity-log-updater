{
  "name": "activity-log-updater",
  "version": "0.1.0",
  "private": true,
  "description": "GitHub Action to generate and commit a configurable activity log.",
  "license": "MIT",
  "type": "commonjs",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test"
  }
}
